<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pennapps2016.payshare">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity android:name=".ui.LoginActivity" ></activity>

        <activity android:name=".ui.EventActivity" android:parentActivityName=".ui.EventsListActivity"/>

        <activity android:name=".ui.EventsListActivity" />

        <activity android:name=".ui.CreateEventActivity" android:parentActivityName=".ui.EventsListActivity"/>
        <activity android:name=".ui.CreateShareActivity" android:parentActivityName=".ui.EventActivity"/>

        <activity android:name=".ui.ShareInfoActivity" android:parentActivityName=".ui.EventActivity"/>
        <activity android:name=".ui.UserProfileActivity" android:parentActivityName=".ui.EventActivity"/>

    </application>

</manifest>
